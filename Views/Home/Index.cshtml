@model IEnumerable<ChapeauHerkansing.Models.Staff>

@{
    ViewData["Title"] = "Users List";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Username</th>
            <th>Role</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Staff staff in Model)
        {
            <tr>
                <td>@staff.FirstName</td>
                <td>@staff.LastName</td>
                <td>@staff.Username</td>
                <td>@staff.Role</td>
            </tr>
        }
    </tbody>
</table>

